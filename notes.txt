clean up console.log

audit JSON.parse, and other `any`s
lint against any

TODO provisioning accounts: some way to make an account in the "set a password" state (+UI)

document minimum node version

ban `any`, fix existing uses

fetch-videos needs a `--verbose`

fetch-videos maybe doesn't need the server running (just save the files? or update the sql directly?)

ensure all fetchs are encapsulated in their own utilities

organize files more generally

has-channel should be channel-info
  null, or name

a 'list of channels' page
  w/ last updated


rename scan.ts to something else, move it under scripts
split out all the scan logic to its own folder w/ docs


FTS5
one version with descriptions
one with subs
one with both
one with neither

todo consistency for node:foo vs foo + lint

should be a different port

get some eslinting

todo UCOGeU-1Fig3rrDjhm9Zs_wg vi hart

TODO add 'service' field from data.json's .webpage_url_domain (default youtube.com)
TODO extra_data in DB schema for forward-compat

handle missing avatars etc (placeholder)

- favicon
- pull out the yt-dlp stuff
- test w/ pinchflat, have sample lifecycle script
- placeholder avatar

affordance for adding specific videos

ability to modify derived account's can-view-channels permissions

all outstanding todos